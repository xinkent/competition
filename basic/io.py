# 数値入力
$N$ = int(readline())

# 数値入力(複数行)
$LIST$ = [int(readline()) for _ in range($N$)]

# 数値入力(リスト)
$LIST_NAME$ = list(map(int, readline().split()))

# 文字列入力
$S$ = $FRONT$str(readline().rstrip().decode('utf-8'))$BACK$

# 文字列入力(複数行)
$LIST$ = [$FRONT$str(readline().rstrip().decode('utf-8'))$BACK$ for _ in range($N$)]

# 文字列入力(リスト)
$LIST$ = [$FRONT$str(readline().rstrip().decode('utf-8'))$BACK$ for _ in range($N$)]